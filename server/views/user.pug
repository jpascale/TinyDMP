extends layout

block content
  h1 #{title}
  hr
  div
  table.table.table-striped
    thead
      tr
        th Usuario
        th IP
        th URL
    tbody
      each visit, i in visits
        tr
          td #{visit.name}
          td #{visit.ip}
          td
            a(href=visit.url) #{visit.url}